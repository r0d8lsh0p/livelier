{
  "name": "livelier",
  "version": "0.1.0",
  "private": true,
  "description": "Livelier — discovers self-hosted live streams from public directories (Owncast directory first) and publishes them to the Nostr network as NIP-53 live events, with optional two-way live chat.",
  "workspaces": [
    "packages/bridge"
  ],
  "scripts": {
    "dev": "npm run dev -w packages/bridge",
    "start": "npm start -w packages/bridge",
    "build": "npm run build -w packages/bridge",
    "test": "npm test -w packages/bridge",
    "typecheck": "npm run typecheck -w packages/bridge",
    "e2e": "npm run e2e -w packages/bridge"
  }
}
