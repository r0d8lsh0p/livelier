{
  "name": "@livelier/site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "livelier.live — the public front page for the Livelier bridge. Static build, live data read from the bridge relays in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "nostr-tools": "^2.19.1"
  },
  "devDependencies": {
    "@noble/hashes": "^1.8.0",
    "@types/node": "^26.2.0",
    "typescript": "~5.3.3",
    "vite": "^5.4.11"
  }
}
